{
  "name": "ashland-market-approach-b",
  "version": "1.0.0",
  "private": true,
  "description": "Ashland Market Heat Map — Approach B (Leaflet + Vanilla JS + D3)",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.0.0"
  }
}
